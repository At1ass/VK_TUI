# VK Tauri Messenger - Roadmap & Implementation Plan

**Дата создания:** 2026-01-05
**Текущий статус:** Базовый мессенджер (текст + просмотр медиа)

---

## 📊 Сравнение TUI vs Tauri GUI

### ✅ Реализовано в обоих
- Авторизация OAuth
- Список диалогов
- Отправка/получение текстовых сообщений
- Редактирование/удаление своих сообщений
- LongPoll для реальных обновлений
- Индикация прочтения
- Индикация "печатает..."
- Поиск по сообщениям (глобальный)
- Ответы (reply)
- Пересылка сообщений

### ⚠️ Только в TUI
- ❌ Отправка фото/файлов
- ❌ Скачивание вложений
- ❌ Vi-подобный интерфейс
- ❌ Командный режим (:command)
- ❌ Локальная фильтрация чатов
- ❌ Справка (help popup)

### ✅ Только в Tauri
- ✅ Визуальное отображение медиа
- ✅ Графический интерфейс
- ✅ Мышь (клик, контекстное меню)
- ✅ Модальные окна

---

## P0: КРИТИЧНЫЕ ФИЧИ (Sprint 1-2)

### 1. ⭐ Отправка файлов и фото
**Статус:** ❌ Не реализовано
**Приоритет:** P0
**Сложность:** 🟢 Низкая (2-3 дня)
**Sprint:** 1

**Почему критично:**
- 90% пользователей отправляют картинки/файлы ежедневно
- Базовая функция любого современного мессенджера
- VK API уже реализован в `vk-api/src/methods/messages.rs:656,720`

**Файлы для модификации:**
```
Backend:
- vk-tauri/src/commands.rs       ← добавить send_photo, send_doc
- vk-core/src/executor/mod.rs    ← обработка SendPhoto, SendDoc

Frontend:
- vk-tauri/ui/src/components/MessageInput.svelte  ← кнопка + file picker
```

**Реализация:**
1. Backend команды `send_photo(peer_id, path)`, `send_doc(peer_id, path)`
2. UI: кнопка "📎" в MessageInput
3. Tauri dialog API для выбора файла
4. Прогресс-бар загрузки (опционально)

---

### 2. ⭐ Скачивание вложений
**Статус:** ❌ Не реализовано
**Приоритет:** P0
**Сложность:** 🟢 Низкая (1-2 дня)
**Sprint:** 1

**Почему критично:**
- Бесполезно видеть файл без возможности сохранить
- Пользователи получают документы, которые нужно скачать

**Файлы для модификации:**
```
Backend:
- vk-tauri/src/commands.rs       ← download_attachment(url, filename)

Frontend:
- vk-tauri/ui/src/components/Message.svelte:93  ← кнопка "↓ Скачать"
```

**Реализация:**
1. HTTP GET запрос к URL вложения
2. Сохранение через Tauri FS API в Downloads
3. Уведомление об успехе/ошибке

---

### 3. Drag & Drop для файлов
**Статус:** ❌ Не реализовано
**Приоритет:** P0
**Сложность:** 🟡 Средняя (2-3 дня)
**Sprint:** 1

**Почему критично:**
- UX стандарт для desktop приложений
- В разы удобнее чем file picker

**Файлы для модификации:**
```
Frontend:
- vk-tauri/ui/src/components/MessageInput.svelte  ← обработка drop
```

**Реализация:**
1. Event handlers: `dragover`, `dragleave`, `drop`
2. Валидация типа файла (изображения vs документы)
3. Превью перед отправкой (опционально)
4. Отправка через существующие команды

**События:**
```javascript
function handleDrop(e) {
  e.preventDefault();
  const files = Array.from(e.dataTransfer.files);

  for (const file of files) {
    if (file.type.startsWith('image/')) {
      await invoke('send_photo', { peerId, path: file.path });
    } else {
      await invoke('send_doc', { peerId, path: file.path });
    }
  }
}
```

---

### 4. Системные уведомления
**Статус:** ❌ Не реализовано
**Приоритет:** P0
**Сложность:** 🟡 Средняя (2-3 дня)
**Sprint:** 2

**Почему критично:**
- Пользователь не видит новые сообщения когда окно свернуто
- Desktop-приложение должно уведомлять

**Файлы для модификации:**
```
Backend:
- vk-tauri/src/state.rs:98  ← VkEvent::NewMessage handler
- Cargo.toml                 ← tauri-plugin-notification

Frontend:
- Нет изменений (всё в Rust)
```

**Реализация:**
1. Проверка фокуса окна
2. Если окно не в фокусе + новое сообщение → уведомление
3. Клик по уведомлению → открыть окно + перейти к чату

**Код:**
```rust
use tauri::api::notification::Notification;

if !window.is_focused()? {
    Notification::new(&app.config().tauri.bundle.identifier)
        .title(&format!("Новое сообщение от {}", user_name))
        .body(&text)
        .show()?;
}
```

---

## P1: ОЧЕНЬ ВАЖНЫЕ ФИЧИ (Sprint 2-4)

### 5. Реакции на сообщения 👍❤️😂
**Статус:** ❌ Не реализовано
**Приоритет:** P1
**Сложность:** 🟡 Средняя (3-4 дня)
**Sprint:** 3

**Почему важно:**
- Современный стандарт общения (Telegram, Discord)
- Быстрый способ ответить без текста
- **VK API уже готов!** `messages.send_reaction`, `messages.get_reactions_assets`

**Файлы для модификации:**
```
Backend:
- vk-core/src/commands/mod.rs:110  ← SendReaction, GetReactionsAssets
- vk-core/src/executor/mod.rs      ← обработка команд
- vk-tauri/src/commands.rs         ← Tauri команды

Frontend:
- vk-tauri/ui/src/components/Message.svelte  ← UI picker реакций
- vk-tauri/ui/src/stores/reactions.js        ← кэш доступных реакций
```

**Реализация:**
1. Загрузка списка доступных реакций при старте
2. UI: hover на сообщение → показать панель реакций
3. Клик → отправить реакцию
4. Отображение реакций под сообщением
5. Список кто поставил (hover/click)

**UI Mock:**
```svelte
<div class="message-reactions">
  {#each message.reactions as reaction}
    <button class="reaction-chip" on:click={() => toggleReaction(reaction.id)}>
      {reaction.emoji} {reaction.count}
    </button>
  {/each}
</div>
```

---

### 6. Стикеры
**Статус:** ❌ Не реализовано
**Приоритет:** P1
**Сложность:** 🟠 Высокая (5-7 дней)
**Sprint:** 4

**Почему важно:**
- Огромная часть коммуникации во ВКонтакте
- Многие пользователи общаются только стикерами
- **VK API готов!** Отправка через attachment

**Файлы для создания:**
```
Backend:
- vk-core/src/commands/mod.rs     ← GetStickers, SendSticker
- vk-core/src/models/sticker.rs   ← новый модуль
- vk-tauri/src/commands.rs        ← Tauri команды

Frontend:
- vk-tauri/ui/src/components/StickerPicker.svelte  ← новый компонент
- vk-tauri/ui/src/stores/stickers.js               ← кэш стикерпаков
```

**Этапы:**
1. Загрузка списка стикерпаков (`messages.getStickers`)
2. Кэширование превью стикеров
3. UI picker с категориями
4. Отправка через `attachment=sticker{id}`
5. Отображение полученных стикеров

**UI Mock:**
```
┌─ Стикеры ──────────────────┐
│ [Популярные] [Мои] [Все]   │
│                            │
│ 😊 😂 ❤️ 👍 🔥 💯          │
│ [превью стикеров 6x3]      │
│                            │
└────────────────────────────┘
```

---

### 7. Закрепление сообщений (Pin)
**Статус:** ❌ Не реализовано
**Приоритет:** P1
**Сложность:** 🟢 Низкая (1-2 дня)
**Sprint:** 3

**Почему важно:**
- Важная информация в беседах (ссылки, адреса, время встречи)
- **VK API готов!** `messages.pin`, `messages.unpin`

**Файлы для модификации:**
```
Backend:
- vk-core/src/commands/mod.rs  ← PinMessage, UnpinMessage
- vk-tauri/src/commands.rs     ← Tauri команды

Frontend:
- vk-tauri/ui/src/components/MessageView.svelte  ← блок закрепленного вверху
- vk-tauri/ui/src/components/Message.svelte      ← кнопка Pin в контекстном меню
```

**Реализация:**
1. Добавить "Закрепить" в контекстное меню Message
2. Sticky блок вверху MessageView с закрепленным сообщением
3. Клик по блоку → скролл к сообщению
4. Кнопка "Открепить" (только для админов в беседах)

**UI Mock:**
```svelte
{#if pinnedMessage}
  <div class="pinned-message-bar">
    <div class="pinned-icon">📌</div>
    <div class="pinned-content" on:click={scrollToPinned}>
      <div class="pinned-author">{pinnedMessage.from_name}</div>
      <div class="pinned-text">{truncate(pinnedMessage.text, 100)}</div>
    </div>
    <button class="unpin-btn" on:click={unpinMessage}>✕</button>
  </div>
{/if}
```

---

### 8. Трей-иконка с badge
**Статус:** ❌ Не реализовано
**Приоритет:** P1
**Сложность:** 🟡 Средняя (2-3 дня)
**Sprint:** 2

**Почему важно:**
- Desktop-стандарт (Telegram, Slack, Discord)
- Видно количество непрочитанных без открытия окна
- Быстрый доступ из трея

**Файлы для модификации:**
```
Backend:
- vk-tauri/src/main.rs          ← SystemTray setup
- vk-tauri/icons/               ← иконки трея (16x16, 32x32, 64x64)
- vk-tauri/tauri.conf.json      ← systemTray config
```

**Реализация:**
1. Создать иконки трея (разные разрешения + с badge)
2. Настроить SystemTray в main.rs
3. Меню: Показать/Скрыть, Выход
4. Обновление badge при новых сообщениях
5. Клик на трей → показать/скрыть окно

**Меню трея:**
```rust
let tray_menu = SystemTrayMenu::new()
    .add_item(CustomMenuItem::new("show_hide", "Показать/Скрыть"))
    .add_separator()
    .add_item(CustomMenuItem::new("unread", "Непрочитанных: 3").disabled())
    .add_separator()
    .add_item(CustomMenuItem::new("quit", "Выход"));
```

---

### 9. Горячие клавиши (расширенные)
**Статус:** ⚠️ Частично (Ctrl+F, Esc)
**Приоритет:** P1
**Сложность:** 🟢 Низкая (1-2 дня)
**Sprint:** 2

**Почему важно:**
- Повышает скорость работы для power users
- Стандарт desktop приложений

**Добавить:**
```
✅ Ctrl+F     - Поиск (есть)
✅ Escape     - Закрыть модалки (есть)
❌ Ctrl+N     - Новое сообщение / Новый чат
❌ Ctrl+W     - Закрыть текущий чат
❌ Ctrl+K     - Быстрый переход к чату (Quick Switcher)
❌ Ctrl+,     - Настройки
❌ Ctrl+Up    - Редактировать последнее сообщение
❌ Ctrl+D     - Удалить выбранное сообщение
❌ Ctrl+Enter - Отправить сообщение (альтернатива Enter)
❌ Alt+↑/↓    - Навигация по чатам
```

**Файлы:**
```
Frontend:
- vk-tauri/ui/src/App.svelte  ← глобальный keydown handler
```

**Реализация:**
```svelte
<svelte:window on:keydown={handleGlobalKeydown} />

<script>
function handleGlobalKeydown(e) {
  if (e.ctrlKey && e.key === 'k') {
    e.preventDefault();
    showQuickSwitcher = true;
  }
  // ...
}
</script>
```

---

### 10. Превью файлов перед отправкой
**Статус:** ❌ Не реализовано
**Приоритет:** P1
**Сложность:** 🟡 Средняя (2-3 дня)
**Sprint:** 4

**Почему важно:**
- Избежать отправки не того файла
- Возможность добавить подпись к фото

**Создать:**
```
Frontend:
- vk-tauri/ui/src/components/AttachmentPreview.svelte  ← модалка
```

**Реализация:**
1. Модальное окно с превью изображения
2. Input для подписи к фото
3. Кнопки: "Отправить" / "Отмена"
4. Поддержка multiple файлов (галерея)

**UI Mock:**
```svelte
<div class="modal-overlay">
  <div class="preview-modal">
    <img src={previewUrl} alt="Preview" />
    <input type="text" placeholder="Добавить подпись..." bind:value={caption} />
    <div class="actions">
      <button on:click={cancel}>Отмена</button>
      <button class="primary" on:click={send}>Отправить</button>
    </div>
  </div>
</div>
```

---

### 11. Индикатор набора текста (typing) - отправка
**Статус:** ⚠️ Частично (получение есть)
**Приоритет:** P1
**Сложность:** 🟢 Низкая (1 день)
**Sprint:** 3

**Почему важно:**
- Показывает собеседнику что вы печатаете
- Стандарт всех современных мессенджеров

**Файлы:**
```
Backend:
- vk-tauri/src/commands.rs  ← set_typing_activity(peer_id)

Frontend:
- vk-tauri/ui/src/components/MessageInput.svelte  ← debounced вызов
```

**Реализация:**
1. При вводе текста → debounce 500ms → отправить typing
2. Повторять каждые 5 секунд пока печатает
3. API: `messages.setActivity(peer_id, "typing")`

**Код:**
```javascript
let typingTimeout;
let typingInterval;

function handleInput() {
  clearTimeout(typingTimeout);

  if (!typingInterval) {
    sendTyping();
    typingInterval = setInterval(sendTyping, 5000);
  }

  typingTimeout = setTimeout(() => {
    clearInterval(typingInterval);
    typingInterval = null;
  }, 5000);
}

async function sendTyping() {
  await invoke('set_typing_activity', { peerId: selectedChat.id });
}
```

---

### 12. Поиск по диалогам (локальная фильтрация)
**Статус:** ❌ Не реализовано (есть в TUI!)
**Приоритет:** P1
**Сложность:** 🟢 Низкая (1-2 дня)
**Sprint:** 4

**Почему важно:**
- Быстро найти нужный чат без глобального поиска
- Есть в TUI, должно быть в GUI

**Файлы:**
```
Frontend:
- vk-tauri/ui/src/components/ChatList.svelte  ← input для фильтра
```

**Реализация:**
1. Input поле вверху ChatList
2. Fuzzy search по chat.title
3. Реактивная фильтрация списка чатов
4. Подсветка совпадений (опционально)

**Код:**
```svelte
<script>
let filterQuery = '';

$: filteredChats = chats.filter(chat =>
  chat.title.toLowerCase().includes(filterQuery.toLowerCase())
);
</script>

<input
  type="search"
  placeholder="Поиск чатов..."
  bind:value={filterQuery}
/>

{#each filteredChats as chat}
  <ChatItem {chat} />
{/each}
```

---

## Sprint План

### ✅ Sprint 1 (Неделя 1): Базовые файлы
**Цель:** Паритет с TUI по работе с файлами

- [ ] 1. Отправка файлов/фото (2-3 дня)
- [ ] 2. Скачивание вложений (1-2 дня)
- [ ] 3. Drag & Drop (2-3 дня)

**Результат:** Полноценная работа с файлами

---

### ✅ Sprint 2 (Неделя 2): Desktop UX
**Цель:** Desktop-приложение с нормальным UX

- [ ] 4. Системные уведомления (2-3 дня)
- [ ] 8. Трей-иконка (2-3 дня)
- [ ] 9. Горячие клавиши (1-2 дня)

**Результат:** Полноценное desktop приложение

---

### ✅ Sprint 3 (Неделя 3): Современные фичи
**Цель:** Современный мессенджер уровня Telegram

- [ ] 5. Реакции (3-4 дня)
- [ ] 7. Pin сообщений (1-2 дня)
- [ ] 11. Typing отправка (1 день)

**Результат:** Современный мессенджер

---

### ✅ Sprint 4 (Недели 4-5): Продвинутые фичи
**Цель:** Полноценный VK Desktop messenger

- [ ] 6. Стикеры (5-7 дней)
- [ ] 10. Превью файлов (2-3 дня)
- [ ] 12. Фильтр диалогов (1-2 дня)

**Результат:** Premium опыт, лучше чем веб-версия VK

---

## P2-P4: Будущие фичи (не в ближайших спринтах)

### P2: Важные (улучшают опыт)
- [ ] Голосовые сообщения (10-14 дней, сложно)
- [ ] Создание/управление беседами (4-5 дней)
- [ ] Цитирование части сообщения (3-4 дня)
- [ ] История редактирований (2-3 дня)
- [ ] Локальная БД/кэш (14-21 день)

### P3: Желательные (nice to have)
- [ ] Папки/метки для диалогов
- [ ] Экспорт истории переписки
- [ ] Кастомизация тем (расширенная)
- [ ] Автозапуск с системой

### P4: Опциональные (низкий приоритет)
- [ ] Мультиаккаунт
- [ ] Offline режим с полным кэшем
- [ ] Группы и сообщества (расширенная работа)
- [ ] Статистика и аналитика

---

## 📊 Матрица приоритетов

```
Критичность ↑
│
│ P0 │ 4. Уведомления        │ 5. Реакции          │ 6. Стикеры         │
│    │ 8. Трей               │ 10. Превью          │ Голосовые          │
│    │                       │                     │ Локальная БД       │
│────┼───────────────────────┼─────────────────────┼────────────────────┤
│ P1 │ 1. Отправка файлов ⭐  │ 3. Drag & Drop      │                    │
│    │ 2. Скачивание ⭐       │ 11. Typing          │                    │
│    │ 7. Pin                │                     │                    │
│    │ 9. Горячие клавиши    │                     │                    │
│    │ 12. Фильтр            │                     │                    │
│────┼───────────────────────┼─────────────────────┼────────────────────┤
│ P2 │ Автозапуск            │ Управление беседами │                    │
│    │                       │ Цитирование         │                    │
│────┼───────────────────────┼─────────────────────┼────────────────────┤
     └───────────────────────┴─────────────────────┴────────────────────┘
       🟢 Низкая               🟡 Средняя             🔴 Высокая
                                                    Сложность →
```

---

## 📈 Прогресс трекинг

### P0 (Критичные): 0/4 (0%)
- [ ] 1. Отправка файлов/фото
- [ ] 2. Скачивание вложений
- [ ] 3. Drag & Drop
- [ ] 4. Системные уведомления

### P1 (Очень важные): 0/8 (0%)
- [ ] 5. Реакции
- [ ] 6. Стикеры
- [ ] 7. Pin сообщений
- [ ] 8. Трей-иконка
- [ ] 9. Горячие клавиши
- [ ] 10. Превью файлов
- [ ] 11. Typing отправка
- [ ] 12. Фильтр диалогов

**Общий прогресс P0+P1:** 0/12 (0%)

---

## 🎯 Следующие шаги

**СЕЙЧАС:** Sprint 1, Фича #1 - Отправка файлов и фото

**Файлы для создания/модификации:**
1. `vk-tauri/src/commands.rs` - добавить `send_photo`, `send_doc`
2. `vk-tauri/ui/src/components/MessageInput.svelte` - UI кнопка вложений

**План реализации:**
1. ✅ Backend команды через vk-api
2. ✅ Tauri dialog API для выбора файла
3. ✅ UI кнопка "📎" в MessageInput
4. ✅ Обработка успеха/ошибки

---

Дата последнего обновления: 2026-01-05
